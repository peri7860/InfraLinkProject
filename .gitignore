# =====================================================================
# InfraLink .gitignore
# ---------------------------------------------------------------------
# [수정 이력] 2026-09-07
#   변경 전 : `*` 로 모든 파일을 제외한 뒤 webapp 의 css/js/pages/components
#             만 화이트리스트로 풀어두는 구조였다.
#             → src/main/java 전체(서블릿·DAO·서비스)가 Git 에 올라가지
#               않아 백엔드 소스가 형상관리에서 통째로 빠져 있었다.
#   변경 후 : "필요한 것만 제외" 하는 일반적인 방식으로 전환.
#             소스는 전부 추적하고, 빌드 산출물/IDE 설정/용량 큰 라이브러리
#             /백업 폴더만 제외한다.
# =====================================================================

# ---------------------------------------------------------------------
# 1. 빌드 산출물 (Eclipse 가 자동 생성 → 추적 불필요)
# ---------------------------------------------------------------------
/build/
/bin/
/target/
*.class
*.war

# ---------------------------------------------------------------------
# 2. IDE / OS 부산물
# ---------------------------------------------------------------------
/.settings/
.classpath
.project
*.iml
/.idea/
/.vscode/
Thumbs.db
.DS_Store

# ---------------------------------------------------------------------
# 3. 외부 라이브러리 (용량이 크고 재다운로드 가능)
#    - 협업 시에는 README.md 의 "라이브러리 준비" 항목을 참고해 각자 배치한다.
#    - ojdbc 는 오라클 라이선스 문제로 저장소에 올리지 않는 것이 원칙이다.
# ---------------------------------------------------------------------
/src/main/webapp/WEB-INF/lib/*.jar

# ---------------------------------------------------------------------
# 4. 민감 정보 (DB 접속 계정)
#    - db.properties 는 각자 로컬 값으로 만들어 쓴다.
#    - 형식은 db.properties.sample 을 참고 (이 파일은 추적한다).
# ---------------------------------------------------------------------
/src/main/java/db.properties

# ---------------------------------------------------------------------
# 5. 업로드 파일 / 로그
# ---------------------------------------------------------------------
/upload/
*.log

# ---------------------------------------------------------------------
# 6. 원본 백업 폴더 (2026-09-07 리팩터링 전 스냅샷)
# ---------------------------------------------------------------------
/_backup_original_*/
